# TWN-Pay

使用 Next.js 16、TypeScript、App Router、Tailwind CSS 與 ESLint 建立的網頁專案。

## 開始使用

```bash
npm install
npm run dev
```

開啟 [http://localhost:3000](http://localhost:3000) 查看網站。

## 指令

- `npm run dev`：啟動開發伺服器
- `npm run build`：建立正式環境版本
- `npm run start`：啟動正式環境伺服器
- `npm run lint`：執行 ESLint
